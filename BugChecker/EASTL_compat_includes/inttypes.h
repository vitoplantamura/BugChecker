//
// some definitions required to compile QuickJS with Windows XP WDK into BugChecker.
// We are reusing "inttypes.h" since it is included in all QJS' files: the goal is to
// make no modifications to the original QuickJS' files.
//

#include "../QuickJSDeclFill.h" // <-- everything here.
